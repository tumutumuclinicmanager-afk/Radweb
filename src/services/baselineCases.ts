import { MedicalCase } from '../types';

/**
 * Baseline/sample cases collection.
 * Empty by default so that only authentic user-created cases in the database are served.
 */
export const DEFAULT_BASELINE_CASES: MedicalCase[] = [];
